---
description: Inicia workflow de nova feature com gate de viabilidade, 3 agentes paralelos e arquitetura
argument-hint: <KEY> ou descrição da feature
---

# Nova Feature

Entrada: $ARGUMENTS

Ativa: `workflow-new-feature`, `layer-boundaries`, `api-contract`, `no-barrel-policy`, `nextjs-react19-architecture`, `naming-conventions`, `core-principles`, `typescript-strict`, e as skills `forms`, `design-system`.

Se input ambíguo, ativar `deep-interview` antes da Phase 1.

Executa o workflow definido em `workflow-new-feature` (Phase 0 Feasibility Gate → Phase 1 Architecture → Phase 2 Implementation → Phase 3 Audit). Não duplicar regras aqui.
