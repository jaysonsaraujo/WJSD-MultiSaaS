---
description: Expande feature existente com nova capacidade sem tratar como greenfield
argument-hint: <KEY> ou descrição da expansão
---

# Expandir Feature

Entrada: $ARGUMENTS

Ativa: `workflow-feature-expansion`, `layer-boundaries`, `api-contract`, `no-barrel-policy`, `nextjs-react19-architecture`, `naming-conventions`, `core-principles`, `typescript-strict`, e as skills `forms`, `design-system`.

Se input ambíguo, ativar `deep-interview` antes da Phase 1.

Executa o workflow definido em `workflow-feature-expansion` (Phase 0 Viability Gate → Phase 1 Expansion Plan → Phase 2 Implementation → Phase 3 Verification). Não duplicar regras aqui.
